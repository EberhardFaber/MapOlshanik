# -*- coding: utf-8 -*-

# Database connection configuration
DB_USER = '*username*'
DB_HOST = '*host address*'
DB_PSW = '*user password*'
DB_NAME = '*database name*'

# Bot's token to connect
TOKEN_BOT = '*bot token*'

# Parameters of proxy connection
PROXY = {
    "IP": '*ip*',
    "PORT": '*port*',
    "LOGIN": '*login*',
    "PSW": '*password*'
}
